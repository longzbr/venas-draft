# 记忆维护纪律（项目继承）

本项目沿用全局 Auto Memory 维护纪律，权威正文位于 `%USERPROFILE%\.codex\memory\feedback\memory-maintenance.md`；项目内不复制完整正文。

后续维护本项目记忆时：先检索并更新旧记录；新结论推翻旧结论时直接修正；索引只保留单行主题摘要；每个子文件只写一个明确主题；遵守 150 行或 20 KB 的日常软上限；不重复 Git、代码和文档中易重新获得的事实；不写入任何秘密；仅在实现支持时按 `user`、`feedback`、`project`、`reference` 分类。
